

single row functions - one resul - one inp

length, concat, substring, upper , lower , curdate,curtime,floor,ceil round

multi row - one resilt- mutliple inpts
max , count,min , sum , avg

group by - 
having

constraints- rules 
not null
unique
primary key
foreign key
check 
auto_increment







joins
to connect two or more table together 
redundancy


inner join - it returns the matching values from both table exclue values that doesn't match

red
green
yellow
blue

red 
yellow
white

inner join
red red
yellow yellow


syntax
select * from table1 inner join table2 on tabl1.id=table2.id;

select * from emp inner join dept on emp.deptno=dept.deptno;
select ename,job, emp.deptno,dept.deptno from emp inner join dept on emp.deptno=dept.deptno;

select e.ename,d.deptno from emp e join dept d on e.deptno=d.deptno;



left join- it returns all the values from the left table and returns matched 
          values only from right table

select e.ename,d.deptno, e.deptno from dept left join emp on dept.deptno=emp.deptno;



right join
it returls all the values from rght table and matchec values from left table
returns null for unmatched columns

select * from emp right join dept on emp.deptno=dept.deptno;


full join union - returns all the values from left and right for matched values
returns null for unmatched columns

select * from emp e left join dept d on e.deptno=d.deptno
 union
select * from emp e right join dept d on e.deptno=d.deptno;



cross join

table 1--

1 
2
3
4
5
6

table 2

1
2
3

1 1
1 2
1 3

2



ename eno  mng no name



select e.ename,e.empno,e2.mgr,e2.ename from 
emp e inner join emp e2 on e.empno=e2.mgr;







